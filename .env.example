# Moore Hill Farm environment variables
# Copy this file to .env.local for local-only testing.
# Never put real client values in this public repository.

# Site
SITE_URL=http://localhost:3000

# Netlify Forms are configured in the Netlify site dashboard.
# Keep form notifications, access, and deployment tokens in Netlify or KM Logic's password manager.
